import type { Metadata } from "next";
export const metadata: Metadata = { title: "Kızma Birader" };
export default function GameLayout({ children }: { children: React.ReactNode }) { return children; }
