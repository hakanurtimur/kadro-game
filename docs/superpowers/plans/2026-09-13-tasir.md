# TAŞIR Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add the two-player TAŞIR hidden-tile column-shifting game as the third game in the existing browser party hub.

**Architecture:** TAŞIR has an isolated pure game engine under `lib/tasir`, with Firebase/local store adapters matching Ludo's room pattern. App Router pages and a mobile-first CSS module render the lobby, RPS start, hidden boards, shift flow and result without coupling to KADRO or Ludo state.

**Tech Stack:** Next.js 15, React 19, TypeScript, Firebase Realtime Database + Anonymous Auth, CSS Modules, node:test.

**Spec:** `docs/superpowers/specs/2026-09-13-tasir-design.md`

## Global Constraints

- Exactly 2 players; host plays normally.
- 4×5 hidden board per player, four copies of digits 0–9, one Joker; every inserted/ejected tile is revealed and never re-closes.
- RPS decides starter; ties repeat.
- 44px minimum actionable mobile targets; safe-area and 100dvh support.
- Existing KADRO and Ludo state/routes remain independent.

---

### Task 1: Pure TAŞIR engine
- [ ] Add engine tests for room size, deal conservation, RPS, same-column continuation, reveal persistence and all-open victory.
- [ ] Implement hidden/revealed board tiles, deterministic deal, RPS resolution, one-shift-at-a-time overflow ownership transitions, same-column continuation and loop release after a full column cycle.
- [ ] Verify engine tests and TypeScript compilation.

### Task 2: Realtime room adapters
- [ ] Add local and Firebase store adapters using `tasirRooms/{code}`.
- [ ] Add Firebase rules permitting authenticated creation, reconnect and lobby-only second-player join.
- [ ] Verify store/rules tests.

### Task 3: Mobile game UI
- [ ] Add `/tasir`, `/tasir/[code]`, mobile-first board, 44px column controls, safe-area layout, slide/flip/pop animations and opened-tile progress.
- [ ] Add the TAŞIR card to the root game hub.
- [ ] Verify route/mobile contract tests and production build.
